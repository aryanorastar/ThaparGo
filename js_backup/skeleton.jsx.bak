import { cn } from ".utils.jsx"

function Skeleton({
  className,
  ...props
}.HTMLAttributes) {
  return (
    <div
      className={cn("animate-pulse rounded-md bg-muted", className)}
      {...props}
    />
  )
}

export { Skeleton }
